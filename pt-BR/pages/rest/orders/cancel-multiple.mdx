---
title: 'Cancelar várias ordens'
description: 'Cancele um lote de ordens por id'
openapi: 'POST /session/cancelMultiple'
---

Cancele um lote de ordens por id. O resultado é posicional com o array `sessionIDs` de entrada e pode conter uma mistura de cancelamentos bem-sucedidos e falhas por ordem.

## Solicitação {#request}

`POST /session/cancelMultiple`

<ParamField body="sessionIDs" type="array" required>
  Array de ids de ordem a cancelar. Todo id deve ser um `ObjectID` válido — se qualquer um deles estiver malformado, a requisição inteira falha com `400`.
</ParamField>

<CodeGroup>
```bash curl
curl -X POST https://api.4casters.io/session/cancelMultiple \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "sessionIDs": [
      "65b036dfb31407c2b6bc1cfe",
      "65b036e0b31407c2b6bc1d08"
    ]
  }'
```
</CodeGroup>

## Resposta {#response}

<ResponseField name="data" type="array">
  Resultados de cancelamento por ordem. Mesmo formato de [Cancelar uma ordem](/pt-BR/pages/rest/orders/cancel-order#response). Em caso de falha, as entradas incluem `{ success: false, reason: "..." }` (sem o campo `sessionID` para rejeições por id inválido / não encontrado / não é sua).
</ResponseField>

### Exemplo {#example}

<CodeGroup>
```json JSON
{
  "data": [
    {
      "success": true,
      "sessionID": "65b036dfb31407c2b6bc1cfe",
      "odds": 125,
      "filled": 0,
      "offered": 0,
      "remaining": 100,
      "gameID": "65f0c3c24ef07d0008abc123"
    },
    { "success": false, "reason": "Not your order" }
  ]
}
```
</CodeGroup>

## Erros {#errors}

| Status | Significado |
| ------ | ------- |
| `400`  | `sessionIDs` ausente ou não é um array, ou algum id malformado. |
