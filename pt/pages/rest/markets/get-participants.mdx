---
title: 'Obter participantes'
description: 'Listar equipas, jogadores e lados de futuros'
openapi: 'GET /exchange/getParticipants'
---

Devolve uma lista sem duplicados de participantes — equipas, jogadores, lados de futuros — em toda a exchange. Use os valores de `participantID` deste endpoint como o campo `side` ao colocar ordens `moneyline` ou `spread`.

## Pedido {#request}

`GET /exchange/getParticipants`

<ParamField query="active" type="string">
  Quando `active=true`, restringe a resposta aos participantes de jogos que começam no futuro. Quando `active=false` (ou omitido), devolve os participantes de todos os jogos.
</ParamField>

<CodeGroup>
```bash curl
curl "https://api.4casters.io/exchange/getParticipants?active=true" \
  -H "Authorization: Bearer YOUR_TOKEN"
```
</CodeGroup>

## Resposta {#response}

<ResponseField name="data.participants" type="array">
  <Expandable title="Participant">
    <ResponseField name="participantID" type="string">Id do participante. Use como `side` ao colocar ordens moneyline / spread.</ResponseField>
    <ResponseField name="longName"      type="string" />
    <ResponseField name="shortName"     type="string" />
    <ResponseField name="league"        type="string" />
    <ResponseField name="sport"         type="string" />
  </Expandable>
</ResponseField>

### Exemplo {#example}

<CodeGroup>
```json JSON
{
  "data": {
    "participants": [
      {
        "participantID": "6021801d14a2fe0012fa0c62",
        "longName":      "Harold Varner III",
        "shortName":     "Varner",
        "league":        "PGA",
        "sport":         "golf"
      }
    ]
  }
}
```
</CodeGroup>
