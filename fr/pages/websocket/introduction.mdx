---
title: 'API d''ordres 4casters'
---

L'API d'ordres 4casters prend en charge le placement d'ordres en temps réel, l'annulation et la gestion de session via une connexion WebSocket brute.

## Endpoints {#endpoints}
- **API d'ordres** : `wss://orders-api.4casters.io/orders/ws`

## Authentification {#auth}

La connexion s'authentifie avec un token 4Casters. Envoyez le token dans
l'en-tête `Authorization` lors du handshake WebSocket. Le serveur déduit
l'utilisateur du token — **pas de nom d'utilisateur dans l'URL**.

### Obtenir un token {#get-a-token}

Connectez-vous avec vos nom d'utilisateur et mot de passe 4Casters en appelant
l'endpoint `/user/login` de l'API REST 4Casters. La réponse inclut un token que
vous pouvez utiliser ici.

Voir [`POST /user/login` dans les docs de l'API REST 4Casters](/fr/pages/rest/authentication).

### Se connecter {#connect}

<CodeGroup>
```javascript JavaScript
const WebSocket = require('ws');
const token = process.env.FOURCASTERS_TOKEN;
const ws = new WebSocket('wss://orders-api.4casters.io/orders/ws', {
  headers: { Authorization: token },
});
```
</CodeGroup>

## Format des messages {#message-format}

Chaque requête client est un 2-uplet `[messageType, payload]`. Chaque payload doit inclure un `requestID` que vous générez ; le serveur le renvoie en écho pour que vous puissiez corréler les réponses.

<CodeGroup>
```json Requête
[
  "messageType",
  {
    "requestID": "YOUR_REQUEST_ID",
    "...": "..."
  }
]
```
</CodeGroup>

Les réponses du serveur sont des objets JSON avec `requestID` plus soit `data` (succès), soit `error` (échec) :

<CodeGroup>
```json Succès
{
  "requestID": "YOUR_REQUEST_ID",
  "data": { "...": "..." }
}
```

```json Erreur
{
  "requestID": "YOUR_REQUEST_ID",
  "error": "human-readable error"
}
```
</CodeGroup>
